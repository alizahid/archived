import { firstName } from '../../../helpers/first-name';
import { module, test } from 'qunit';

module('Unit | Helper | first name');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = firstName(42);
  assert.ok(result);
});
