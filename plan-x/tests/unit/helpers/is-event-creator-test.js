import { isEventCreator } from '../../../helpers/is-event-creator';
import { module, test } from 'qunit';

module('Unit | Helper | is event creator');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = isEventCreator(42);
  assert.ok(result);
});
