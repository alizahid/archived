import { eventTags } from '../../../helpers/event-tags';
import { module, test } from 'qunit';

module('Unit | Helper | event tags');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = eventTags(42);
  assert.ok(result);
});
